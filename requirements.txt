# Core
numpy>=1.20.0
opencv-python>=4.5.5
pandas>=1.3.0
pyyaml>=6.0

# Deep learning
torch>=1.10.0
torchvision>=0.11.0

# Object detection / tracking
ultralytics>=8.0.0
supervision>=0.27.0
lapx>=0.5.2

# Pose estimation (forehand/backhand from body geometry)
# Note: mediapipe pulls in opencv-contrib-python 5.x, which supersedes opencv-python
# above and is stricter about matrix shapes than 4.x — see utils/kalman_smoother.py.
mediapipe>=1.0.0

# Model weight downloads
# huggingface-hub fetches the fine-tuned court model; gdown fetches the upstream
# TrackNet weights, which we point at rather than rehost. Both are required by
# scripts/download_models.py, i.e. by the documented first-run path.
huggingface-hub>=0.20.0
gdown>=4.7.1

# Broadcast transfer test only (eval/validate_on_broadcast_images.py).
# Note: roboflow pulls in opencv-python-headless, which has no GUI functions and
# silently overwrites the opencv-python/opencv-contrib-python build above, whichever
# was installed last. tools/label_shots.py then fails with "namedWindow ... not
# implemented" even though `import cv2` itself works fine. If that happens:
#   pip uninstall opencv-python-headless -y
#   pip install --force-reinstall --no-deps opencv-contrib-python
roboflow>=1.1.0

# Testing
pytest>=7.0.0
